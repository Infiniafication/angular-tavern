export class Hero {
  id: number; // 1 to 4
  name: string;
  level: number; //1 to 30
  heroClass: string;
  spc: string; //Class Specialization
  maxHealth: number;
  maxArcana: number;
  maxFatigue: number;
  curHealth: number;
  curArcana: number;
  curFatigue: number;
  status:string;
}